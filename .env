# Base URL for callbacks/webhook registration.
APP_BASE_URL=http://localhost:8000

# Telegram bot token from @BotFather.
TG_BOT_TOKEN=8588125704:AAEZb8KEDsrHTuEdrSIhAJSI6KTSB56eCtU
# Secret path segment for POST /tg/webhook/{secret}.
TG_WEBHOOK_SECRET=change_me
# true => webhook mode, false => polling mode via python -m app.telegram.polling
USE_TG_WEBHOOK=false
# Optional CSV list of allowed chat IDs. Empty = accept all.
TG_ALLOWED_CHAT_IDS=

# TikTok developer app credentials.
TIKTOK_CLIENT_KEY=df
TIKTOK_CLIENT_SECRET=fdsds
TIKTOK_REDIRECT_URI=http://localhost:8000/tiktok/auth/callback

# Default posting mode for new accounts: draft | direct
POSTING_MODE=draft
# If direct publish fails due scope/permission, fallback to draft upload.
FALLBACK_TO_DRAFT=true
s
# Caption behavior.
APPEND_HASHTAGS=#telegram #tiktok
CAPTION_TEMPLATE=From TG: {text}

# Storage.
STORAGE_DB_PATH=./data/app.db
MEDIA_STORAGE_PATH=./data/media

# Telegram media_group buffering.
MEDIA_GROUP_FLUSH_SECONDS=3

# Slideshow conversion settings for photo fallback.
SLIDE_SECONDS=2
SLIDESHOW_FPS=30

# true => try TikTok photo/carousel endpoint first; false => always ffmpeg->video.
ENABLE_PHOTO_API=false

# Rate limit per TikTok account.
RATE_LIMIT_PER_MINUTE=6

# Optional chat mapping JSON:
# {"-1001234567890":["acc1","acc2"],"-100999":["acc2"]}
TG_TO_TIKTOK_MAPPING_JSON=

